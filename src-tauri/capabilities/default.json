{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set fuer das Haupt-Fenster (M1-Welle 2). Beschraenkt sich auf core:default; Plugin-Permissions werden in spaeteren Wellen ergaenzt, sobald Adapter sie brauchen (z. B. fs fuer Projektpersistenz in M3).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
